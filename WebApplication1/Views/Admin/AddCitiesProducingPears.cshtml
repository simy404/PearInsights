@model WebApplication1.Models.CitiesProducingPearsViewModel

<section class="container d-flex flex-column justify-content-between min-vh-100 mx-auto">
    <div class="col-6">
        <div class="">
            <h1 class="mb-5 ">Veri Ekle</h1>
        </div>
        @using (Html.BeginForm("AddCitiesProducingPears", "Admin", FormMethod.Post))
        {
            <div class="row mb-2">
                <div class="col-md-2">
                    @Html.LabelFor(x => x.City, new { @Class = "form-label" })
                </div>
                <div class="col-md-4">
                    @Html.TextBoxFor(x => x.City, new { @class = "form-control" })
                </div>
                <div class="col-md-4">
                    @Html.ValidationMessageFor(x => x.City)
                </div>
            </div>
            <button type="submit" class="btn btn-success btn-lg">Ekle</button>
        }
    </div>

</section>

